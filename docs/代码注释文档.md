# 期末复习刷题系统代码注释文档

## 1. 文档说明

本文档对 `D:\code\project-1` 项目中的核心代码进行注释说明，重点解释主要类、方法、业务流程和关键实现逻辑。本文档不替代源码注释，而是作为代码阅读、项目答辩和维护开发的辅助说明。

## 2. 后端启动与配置代码

### 2.1 后端启动类

文件：

```text
server/src/main/java/com/examreview/ExamReviewApplication.java
```

作用：

- Spring Boot 后端服务启动入口。
- 扫描 `com.examreview` 包下的 Controller、Service、Mapper、配置类等组件。
- 启动内嵌 Web 容器，默认监听 `8080` 端口。

典型代码说明：

```java
@SpringBootApplication
public class ExamReviewApplication {
    public static void main(String[] args) {
        SpringApplication.run(ExamReviewApplication.class, args);
    }
}
```

注释：

- `@SpringBootApplication` 是组合注解，包含自动配置、组件扫描和配置类声明。
- `SpringApplication.run()` 用于启动 Spring Boot 应用。

### 2.2 安全配置类

文件：

```text
server/src/main/java/com/examreview/config/SecurityConfig.java
```

作用：

- 配置 Spring Security。
- 设置哪些接口可以匿名访问。
- 配置 JWT 过滤器。
- 配置跨域访问。
- 配置密码加密器。

关键代码注释：

```java
@Bean
public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
    http
        // 启用 CORS，允许前端跨域访问后端接口
        .cors(cors -> cors.configurationSource(corsConfigurationSource()))

        // 前后端分离项目通常关闭 CSRF
        .csrf(csrf -> csrf.disable())

        // 使用 JWT 后，服务端不保存 Session
        .sessionManagement(session -> session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))

        // 配置接口访问权限
        .authorizeHttpRequests(auth -> auth
            // 放行 OPTIONS 预检请求
            .requestMatchers(org.springframework.http.HttpMethod.OPTIONS, "/**").permitAll()

            // 登录和注册接口无需登录
            .requestMatchers("/api/auth/login", "/api/auth/register").permitAll()

            // 其他所有接口必须登录
            .anyRequest().authenticated()
        )

        // 将 JWT 过滤器放到用户名密码过滤器之前
        .addFilterBefore(jwtAuthenticationFilter, UsernamePasswordAuthenticationFilter.class);

    return http.build();
}
```

```java
@Bean
public PasswordEncoder passwordEncoder() {
    // 使用 BCrypt 对用户密码加密
    return new BCryptPasswordEncoder();
}
```

## 3. 认证授权相关代码

### 3.1 JWT 过滤器

文件：

```text
server/src/main/java/com/examreview/security/JwtAuthenticationFilter.java
```

作用：

- 每次请求进入后端时，尝试从请求头读取 JWT。
- 校验 JWT 是否有效。
- 校验成功后，把用户信息写入 Spring Security 上下文。
- 后续 Controller 和 Service 可以通过 `SecurityUtil` 获取当前用户。

关键代码注释：

```java
@Override
protected void doFilterInternal(HttpServletRequest request,
                                HttpServletResponse response,
                                FilterChain filterChain) throws ServletException, IOException {
    // 从请求头 Authorization 中提取 Token
    String token = getTokenFromRequest(request);

    // Token 不为空且校验通过
    if (StringUtils.hasText(token) && jwtUtil.validateToken(token)) {
        // 从 Token 中解析用户名
        String username = jwtUtil.getUsernameFromToken(token);

        // 根据用户名加载用户详情，包括用户 ID、密码、角色等信息
        UserDetails userDetails = userDetailsService.loadUserByUsername(username);

        // 构造 Spring Security 认证对象
        UsernamePasswordAuthenticationToken authentication =
                new UsernamePasswordAuthenticationToken(
                        userDetails, null, userDetails.getAuthorities()
                );

        // 设置请求细节，如 IP、Session 等
        authentication.setDetails(new WebAuthenticationDetailsSource().buildDetails(request));

        // 将认证信息写入当前请求上下文
        SecurityContextHolder.getContext().setAuthentication(authentication);
    }

    // 放行请求，继续执行后续过滤器或 Controller
    filterChain.doFilter(request, response);
}
```

```java
private String getTokenFromRequest(HttpServletRequest request) {
    // 读取 Authorization 请求头
    String bearerToken = request.getHeader("Authorization");

    // 判断是否为 Bearer Token 格式
    if (StringUtils.hasText(bearerToken) && bearerToken.startsWith("Bearer ")) {
        // 去掉 Bearer 前缀，只返回实际 Token
        return bearerToken.substring(7);
    }
    return null;
}
```

### 3.2 JWT 工具类

文件：

```text
server/src/main/java/com/examreview/util/JwtUtil.java
```

作用：

- 生成 JWT。
- 从 JWT 中解析用户 ID。
- 从 JWT 中解析用户名。
- 校验 JWT 是否有效。

关键代码注释：

```java
public String generateToken(Long userId, String username) {
    // 当前时间
    Date now = new Date();

    // 过期时间 = 当前时间 + 配置的有效期
    Date expiryDate = new Date(now.getTime() + expiration);

    // 构造 JWT
    return Jwts.builder()
            // subject 存储用户 ID
            .subject(String.valueOf(userId))

            // 自定义 claim 存储用户名
            .claim("username", username)

            // 签发时间
            .issuedAt(now)

            // 过期时间
            .expiration(expiryDate)

            // 使用密钥签名
            .signWith(key)

            // 生成字符串 Token
            .compact();
}
```

```java
public boolean validateToken(String token) {
    try {
        // 只要能正常解析，说明 Token 格式、签名、过期时间均通过
        parseToken(token);
        return true;
    } catch (SecurityException | MalformedJwtException | ExpiredJwtException
             | UnsupportedJwtException | IllegalArgumentException e) {
        // 解析失败说明 Token 无效
        return false;
    }
}
```

### 3.3 当前用户工具类

文件：

```text
server/src/main/java/com/examreview/util/SecurityUtil.java
```

作用：

- 从 Spring Security 上下文中获取当前登录用户。
- 获取当前用户 ID。
- 判断当前用户是否管理员。

关键代码注释：

```java
public static Long getCurrentUserId() {
    // 获取当前请求的认证对象
    Authentication authentication = SecurityContextHolder.getContext().getAuthentication();

    // 判断认证对象中保存的用户主体是否为 UserPrincipal
    if (authentication != null && authentication.getPrincipal() instanceof UserPrincipal) {
        Long userId = ((UserPrincipal) authentication.getPrincipal()).getId();
        if (userId == null) {
            throw new RuntimeException("当前用户ID为空，请重新登录");
        }
        return userId;
    }

    // 未获取到认证信息，说明用户未登录或 Token 无效
    throw new RuntimeException("未获取到用户认证信息，请重新登录");
}
```

```java
public static boolean isAdmin() {
    Authentication authentication = SecurityContextHolder.getContext().getAuthentication();
    if (authentication != null && authentication.getPrincipal() instanceof UserPrincipal) {
        // 判断用户角色是否为 admin
        return ((UserPrincipal) authentication.getPrincipal()).isAdmin();
    }
    return false;
}
```

## 4. 登录注册模块代码

### 4.1 登录注册接口

文件：

```text
server/src/main/java/com/examreview/controller/AuthController.java
```

作用：

- 提供用户登录接口。
- 提供用户注册接口。
- 提供获取当前用户信息接口。
- 提供修改密码接口。

关键代码注释：

```java
@PostMapping("/login")
public ApiResponse<LoginResponse> login(@Valid @RequestBody LoginRequest request) {
    // 调用认证业务层完成账号密码校验和 Token 生成
    LoginResponse response = authService.login(request);

    // 使用统一响应格式返回登录结果
    return ApiResponse.ok(response, "登录成功");
}
```

```java
@PostMapping("/register")
public ApiResponse<LoginResponse> register(@Valid @RequestBody RegisterRequest request) {
    // 调用注册业务，完成用户名校验、密码加密和用户入库
    LoginResponse response = authService.register(request);
    return ApiResponse.ok(response, "注册成功");
}
```

```java
@GetMapping("/me")
public ApiResponse<LoginResponse> me(@AuthenticationPrincipal UserPrincipal principal) {
    // 从 Spring Security 注入的 principal 中获取当前用户 ID
    LoginResponse response = authService.getCurrentUser(principal.getId());
    return ApiResponse.ok(response);
}
```

### 4.2 登录注册业务实现

文件：

```text
server/src/main/java/com/examreview/service/impl/AuthServiceImpl.java
```

作用：

- 登录时校验用户名和密码。
- 注册时检查用户名是否重复。
- 使用 BCrypt 加密密码。
- 登录或注册成功后生成 JWT。

登录代码注释：

```java
@Override
public LoginResponse login(LoginRequest request) {
    // 使用 Spring Security 认证管理器校验用户名和密码
    Authentication authentication = authenticationManager.authenticate(
            new UsernamePasswordAuthenticationToken(
                    request.getUsername(), request.getPassword()
            )
    );

    // 认证成功后获取用户主体
    UserPrincipal principal = (UserPrincipal) authentication.getPrincipal();

    // 根据用户 ID 和用户名生成 JWT
    String token = jwtUtil.generateToken(principal.getId(), principal.getUsername());

    // 查询用户完整信息，用于返回昵称和角色
    User user = userMapper.selectById(principal.getId());

    // 返回 token、用户 ID、用户名、昵称、角色
    return new LoginResponse(
            token, principal.getId(), principal.getUsername(),
            user != null ? user.getNickname() : principal.getUsername(),
            user != null && user.getRole() != null ? user.getRole() : "user"
    );
}
```

注册代码注释：

```java
@Override
public LoginResponse register(RegisterRequest request) {
    // 查询用户名是否已经存在
    Long count = userMapper.selectCount(
            new LambdaQueryWrapper<User>().eq(User::getUsername, request.getUsername())
    );

    // 用户名重复时抛出业务异常
    if (count > 0) {
        throw new BusinessException("用户名已存在");
    }

    // 构造新用户对象
    User user = new User();
    user.setUsername(request.getUsername());

    // 密码加密后再保存到数据库
    user.setPassword(passwordEncoder.encode(request.getPassword()));
    user.setNickname(request.getNickname() != null ? request.getNickname() : request.getUsername());

    // 插入数据库
    userMapper.insert(user);

    // 注册成功后直接生成 Token，实现注册后自动登录
    String token = jwtUtil.generateToken(user.getId(), user.getUsername());

    return new LoginResponse(
            token, user.getId(), user.getUsername(), user.getNickname(),
            user.getRole() != null ? user.getRole() : "user"
    );
}
```

## 5. 科目管理模块代码

### 5.1 科目接口

文件：

```text
server/src/main/java/com/examreview/controller/SubjectController.java
```

作用：

- 提供科目的增删改查接口。
- 所有接口都通过 `SecurityUtil.getCurrentUserId()` 获取当前用户。

关键代码注释：

```java
@GetMapping
public ApiResponse<List<Subject>> getAll() {
    // 查询当前用户的科目列表
    return ApiResponse.ok(subjectService.getAll(SecurityUtil.getCurrentUserId()));
}
```

```java
@PostMapping
public ApiResponse<Subject> create(@RequestBody Subject subject) {
    // 创建科目时传入当前用户 ID，用于绑定 user_id
    return ApiResponse.ok(subjectService.create(subject, SecurityUtil.getCurrentUserId()), "创建成功");
}
```

```java
@DeleteMapping("/{id}")
public ApiResponse<Void> delete(@PathVariable Integer id) {
    // 删除前由 Service 检查科目是否存在、是否属于当前用户、是否有关联章节
    subjectService.delete(id, SecurityUtil.getCurrentUserId());
    return ApiResponse.ok(null, "删除成功");
}
```

### 5.2 科目业务

文件：

```text
server/src/main/java/com/examreview/service/impl/SubjectServiceImpl.java
```

核心逻辑：

- 查询科目时按 `user_id` 隔离。
- 管理员可以跳过用户过滤。
- 创建和修改时校验科目名称。
- 删除时检查是否存在章节。

关键代码注释：

```java
@Override
public List<Subject> getAll(Long userId) {
    return subjectMapper.selectList(
            new LambdaQueryWrapper<Subject>()
                    // 普通用户只查询自己的科目，管理员不加 user_id 条件
                    .eq(!SecurityUtil.isAdmin(), Subject::getUserId, userId)

                    // 按排序号升序展示
                    .orderByAsc(Subject::getSortOrder));
}
```

```java
@Override
public Subject create(Subject subject, Long userId) {
    // 校验科目名称不能为空
    if (subject.getName() == null || subject.getName().trim().isEmpty()) {
        throw new BusinessException("科目名称不能为空");
    }

    // 校验长度
    if (subject.getName().trim().length() > 50) {
        throw new BusinessException("科目名称不能超过50个字符");
    }

    // 检查当前用户下是否存在同名科目
    Long count = subjectMapper.selectCount(
            new LambdaQueryWrapper<Subject>()
                    .eq(!SecurityUtil.isAdmin(), Subject::getUserId, userId)
                    .eq(Subject::getName, subject.getName().trim()));

    if (count > 0) {
        throw new BusinessException("科目名称已存在");
    }

    // 绑定当前用户
    subject.setUserId(userId);

    // 插入数据库
    subjectMapper.insert(subject);
    return subject;
}
```

```java
@Override
@Transactional
public void delete(Integer id, Long userId) {
    // 校验科目存在并且属于当前用户
    getById(id, userId);

    // 查询该科目下是否存在章节
    Long chapterCount = chapterMapper.selectCount(
            new LambdaQueryWrapper<Chapter>().eq(Chapter::getSubjectId, id));

    // 有章节时禁止删除，避免产生孤立数据
    if (chapterCount > 0) {
        throw new BusinessException("该科目下还有 " + chapterCount + " 个章节，请先删除章节");
    }

    subjectMapper.deleteById(id);
}
```

## 6. 章节管理模块代码

### 6.1 章节接口

文件：

```text
server/src/main/java/com/examreview/controller/ChapterController.java
```

作用：

- 提供章节列表、新增、编辑、删除接口。
- 章节挂在科目下，因此新增章节时需要 `subjectId`。

关键代码注释：

```java
@GetMapping("/subjects/{subjectId}/chapters")
public ApiResponse<List<Chapter>> getBySubject(@PathVariable Integer subjectId) {
    // 查询当前科目下属于当前用户的章节
    return ApiResponse.ok(chapterService.getBySubjectId(subjectId, SecurityUtil.getCurrentUserId()));
}
```

```java
@PostMapping("/subjects/{subjectId}/chapters")
public ApiResponse<Chapter> create(@PathVariable Integer subjectId, @RequestBody Chapter chapter) {
    // 从路径中获取 subjectId 并设置到章节对象
    chapter.setSubjectId(subjectId);

    // 创建章节
    return ApiResponse.ok(chapterService.create(chapter, SecurityUtil.getCurrentUserId()), "创建成功");
}
```

### 6.2 章节业务

文件：

```text
server/src/main/java/com/examreview/service/impl/ChapterServiceImpl.java
```

核心逻辑：

- 按科目 ID 查询章节。
- 新增章节时自动计算排序号。
- 删除章节时检查题目和错题记录。

关键代码注释：

```java
@Override
@Transactional
public Chapter create(Chapter chapter, Long userId) {
    // 章节名称不能为空
    if (chapter.getName() == null || chapter.getName().trim().isEmpty()) {
        throw new BusinessException("章节名称不能为空");
    }

    // 查询当前科目下排序号最大的章节
    List<Chapter> existing = chapterMapper.selectList(
            new LambdaQueryWrapper<Chapter>()
                    .eq(Chapter::getSubjectId, chapter.getSubjectId())
                    .eq(!SecurityUtil.isAdmin(), Chapter::getUserId, userId)
                    .orderByDesc(Chapter::getSortOrder)
                    .last("LIMIT 1"));

    // 新章节排序号 = 当前最大排序号 + 1
    chapter.setSortOrder(existing.isEmpty() ? 1 : existing.get(0).getSortOrder() + 1);

    // 绑定当前用户
    chapter.setUserId(userId);

    chapterMapper.insert(chapter);
    return chapter;
}
```

```java
@Override
public void delete(Integer id, Long userId) {
    // 查询章节，普通用户只能删除自己的章节
    Chapter existing = chapterMapper.selectOne(
            new LambdaQueryWrapper<Chapter>()
                    .eq(Chapter::getId, id)
                    .eq(!SecurityUtil.isAdmin(), Chapter::getUserId, userId));

    if (existing == null) {
        throw new BusinessException("章节不存在");
    }

    // 删除章节前检查是否有关联题目
    Long questionCount = questionMapper.selectCount(
            new LambdaQueryWrapper<Question>().eq(Question::getChapterId, id));
    if (questionCount > 0) {
        throw new BusinessException("该章节下还有 " + questionCount + " 道题目，请先删除题目再删除章节");
    }

    // 删除章节前检查是否有关联错题记录
    Long wrongCount = wrongQuestionMapper.selectCount(
            new LambdaQueryWrapper<WrongQuestion>().eq(WrongQuestion::getChapterId, id));
    if (wrongCount > 0) {
        throw new BusinessException("该章节下还有 " + wrongCount + " 条错题记录，请先清理后再删除章节");
    }

    chapterMapper.deleteById(id);
}
```

## 7. 管理后台代码

文件：

```text
server/src/main/java/com/examreview/controller/AdminController.java
```

作用：

- 管理员查看用户列表。
- 统计每个用户的科目数、题目数、考试数、错题数、答题数。
- 管理员删除普通用户及其关联数据。

关键代码注释：

```java
@GetMapping("/users")
public ApiResponse<List<Map<String, Object>>> getUsers() {
    // 只有管理员可以访问
    if (!SecurityUtil.isAdmin()) {
        throw new BusinessException(403, "无权访问");
    }

    // 查询所有用户
    List<User> users = userMapper.selectList(null);
    List<Map<String, Object>> result = new ArrayList<>();

    for (User u : users) {
        Map<String, Object> item = new LinkedHashMap<>();

        // 用户基础信息
        item.put("id", u.getId());
        item.put("username", u.getUsername());
        item.put("nickname", u.getNickname());
        item.put("role", u.getRole());
        item.put("createdAt", u.getCreatedAt());

        // 用户关联数据统计
        item.put("subjectCount", subjectMapper.selectCount(
                new LambdaQueryWrapper<Subject>().eq(Subject::getUserId, u.getId())));
        item.put("questionCount", questionMapper.selectCount(
                new LambdaQueryWrapper<Question>().eq(Question::getUserId, u.getId())));
        item.put("examCount", examRecordMapper.selectCount(
                new LambdaQueryWrapper<ExamRecord>().eq(ExamRecord::getUserId, u.getId())));
        item.put("wrongCount", wrongQuestionMapper.selectCount(
                new LambdaQueryWrapper<WrongQuestion>().eq(WrongQuestion::getUserId, u.getId())));
        item.put("answerCount", answerRecordMapper.selectCount(
                new LambdaQueryWrapper<AnswerRecord>().eq(AnswerRecord::getUserId, u.getId())));

        result.add(item);
    }
    return ApiResponse.ok(result);
}
```

删除用户代码说明：

```java
@DeleteMapping("/users/{id}")
@Transactional
public ApiResponse<Void> deleteUser(@PathVariable Long id) {
    // 校验管理员权限
    if (!SecurityUtil.isAdmin()) {
        throw new BusinessException(403, "无权访问");
    }

    // 管理员不能删除自己
    if (SecurityUtil.getCurrentUserId().equals(id)) {
        throw new BusinessException("不能删除自己的账号");
    }

    // 查询目标用户
    User user = userMapper.selectById(id);
    if (user == null) {
        throw new BusinessException("用户不存在");
    }

    // 禁止删除管理员账号
    if ("admin".equals(user.getRole())) {
        throw new BusinessException("不能删除管理员账号");
    }

    // 依次删除该用户关联的业务数据
    answerRecordMapper.delete(new LambdaQueryWrapper<AnswerRecord>().eq(AnswerRecord::getUserId, id));
    wrongQuestionMapper.delete(new LambdaQueryWrapper<WrongQuestion>().eq(WrongQuestion::getUserId, id));
    examRecordMapper.delete(new LambdaQueryWrapper<ExamRecord>().eq(ExamRecord::getUserId, id));

    // 删除试卷及试卷题目关联
    List<ExamPaper> papers = examPaperMapper.selectList(
            new LambdaQueryWrapper<ExamPaper>().eq(ExamPaper::getUserId, id));
    for (ExamPaper p : papers) {
        examQuestionMapper.delete(
                new LambdaQueryWrapper<ExamQuestion>().eq(ExamQuestion::getExamId, p.getId()));
        examPaperMapper.deleteById(p.getId());
    }

    // 删除题目、章节、科目、用户
    questionMapper.delete(new LambdaQueryWrapper<Question>().eq(Question::getUserId, id));
    chapterMapper.delete(new LambdaQueryWrapper<Chapter>().eq(Chapter::getUserId, id));
    subjectMapper.delete(new LambdaQueryWrapper<Subject>().eq(Subject::getUserId, id));
    userMapper.deleteById(id);

    return ApiResponse.ok(null, "用户及其数据已删除");
}
```

## 8. 统一响应与异常处理代码

### 8.1 统一响应类

文件：

```text
server/src/main/java/com/examreview/dto/ApiResponse.java
```

作用：

- 统一所有接口的返回格式。
- 成功响应使用 `ok()`。
- 失败响应使用 `fail()`。

代码注释：

```java
@Data
@NoArgsConstructor
@AllArgsConstructor
public class ApiResponse<T> {

    // 状态码，成功为 200，失败为业务错误码
    private int code;

    // 返回数据
    private T data;

    // 返回消息
    private String message;

    public static <T> ApiResponse<T> ok(T data) {
        return new ApiResponse<>(200, data, "success");
    }

    public static <T> ApiResponse<T> ok(T data, String message) {
        return new ApiResponse<>(200, data, message);
    }

    public static <T> ApiResponse<T> fail(int code, String message) {
        return new ApiResponse<>(code, null, message);
    }
}
```

### 8.2 全局异常处理

文件：

```text
server/src/main/java/com/examreview/exception/GlobalExceptionHandler.java
```

作用：

- 捕获业务异常。
- 捕获参数校验异常。
- 捕获登录认证异常。
- 捕获 JSON 请求体解析异常。
- 捕获未处理异常。

关键代码注释：

```java
@ExceptionHandler(BusinessException.class)
public ApiResponse<Void> handleBusinessException(BusinessException e) {
    // 业务异常只记录 warn，不打印完整堆栈
    log.warn("业务异常: {}", e.getMessage());

    // 返回业务异常中的 code 和 message
    return ApiResponse.fail(e.getCode(), e.getMessage());
}
```

```java
@ExceptionHandler(Exception.class)
public ApiResponse<Void> handleException(Exception e) {
    // 未知异常打印完整错误日志，方便排查
    log.error("服务器内部错误", e);

    // 返回统一的 500 错误提示，避免泄露内部细节
    return ApiResponse.fail(500, "服务器内部错误，请稍后重试");
}
```

## 9. 前端 API 封装代码

文件：

```text
client/composables/useApi.js
```

作用：

- 统一封装前端请求。
- 自动添加 JWT。
- 统一处理后端响应格式。
- 登录过期时自动跳转登录页。

关键代码注释：

```js
export const useApi = () => {
  // 读取 Nuxt 运行时配置中的 API 地址
  const config = useRuntimeConfig()
  const baseURL = config.public.apiBase

  const request = async (url, options = {}) => {
    const method = (options.method || 'GET').toUpperCase()
    const headers = { ...options.headers }

    // 客户端环境下，从 localStorage 读取 Token
    if (process.client) {
      const token = localStorage.getItem('auth_token')
      if (token) {
        // 自动注入 Authorization 请求头
        headers['Authorization'] = `Bearer ${token}`
      }
    }

    // 对有请求体的方法设置 JSON 类型
    if (['POST', 'PUT', 'PATCH', 'DELETE'].includes(method)) {
      headers['Content-Type'] = 'application/json'
    }

    try {
      // 发送请求
      const result = await $fetch(`${baseURL}${url}`, {
        method,
        headers,
        body: options.body,
        params: options.params,
      })

      // 后端业务失败时弹出错误
      if (result.code !== 200) {
        ElMessage.error(result.message || '请求失败')
        throw new Error(result.message)
      }

      // 只返回 data，页面不用关心 code/message 外壳
      return result.data
    } catch (err) {
      // 401 表示未登录或登录过期
      if (err.response?.status === 401) {
        if (process.client) {
          localStorage.removeItem('auth_token')
          localStorage.removeItem('auth_user')
        }
        ElMessage.error('登录已过期，请重新登录')

        // 跳转登录页，并记录原访问路径
        if (process.client && !window.location.pathname.includes('/login')) {
          window.location.href = '/login?redirect=' + encodeURIComponent(window.location.pathname)
        }
        throw err
      }

      throw err
    }
  }

  return {
    get: (url, params) => request(url, { method: 'GET', params }),
    post: (url, body) => request(url, { method: 'POST', body }),
    put: (url, body) => request(url, { method: 'PUT', body }),
    delete: (url, body) => request(url, { method: 'DELETE', body }),
  }
}
```

## 10. 前端登录状态代码

文件：

```text
client/stores/auth.js
```

作用：

- 管理登录 Token。
- 管理当前用户信息。
- 登录、注册、退出。
- 从 `localStorage` 恢复登录状态。

关键代码注释：

```js
export const useAuthStore = defineStore('auth', () => {
  // 当前登录 Token
  const token = ref(null)

  // 当前用户信息
  const user = ref(null)

  // 是否已登录
  const isAuthenticated = computed(() => !!token.value)

  // 页面刷新后从 localStorage 恢复登录态
  const init = () => {
    if (process.client) {
      const savedToken = localStorage.getItem('auth_token')
      const savedUser = localStorage.getItem('auth_user')
      if (savedToken) {
        token.value = savedToken
        try {
          user.value = JSON.parse(savedUser)
        } catch (e) {
          user.value = null
        }
      }
    }
  }
})
```

登录方法注释：

```js
const login = async (username, password) => {
  try {
    const api = useApi()

    // 调用后端登录接口
    const data = await api.post('/auth/login', { username, password })

    // 保存 Token
    token.value = data.token

    // 保存用户信息
    user.value = {
      userId: data.userId,
      username: data.username,
      nickname: data.nickname,
      role: data.role || 'user',
    }

    // 持久化到 localStorage
    if (process.client) {
      localStorage.setItem('auth_token', data.token)
      localStorage.setItem('auth_user', JSON.stringify(user.value))
    }

    ElMessage.success('登录成功')
    return true
  } catch (err) {
    return false
  }
}
```

退出方法注释：

```js
const logout = () => {
  // 清空内存中的登录态
  token.value = null
  user.value = null

  // 清空浏览器本地存储
  if (process.client) {
    localStorage.removeItem('auth_token')
    localStorage.removeItem('auth_user')
  }

  ElMessage.success('已退出登录')
}
```

## 11. 前端路由守卫代码

文件：

```text
client/middleware/auth.global.js
```

作用：

- 未登录用户访问业务页面时跳转登录页。
- 已登录用户访问登录/注册页时跳转首页。

关键代码注释：

```js
export default defineNuxtRouteMiddleware((to) => {
  // 不需要登录即可访问的页面
  const publicRoutes = ['/login', '/register']

  // 只在客户端检查 localStorage
  if (process.client) {
    const token = localStorage.getItem('auth_token')

    // 未登录且访问的不是公开页面，跳转登录页
    if (!token && !publicRoutes.includes(to.path)) {
      return navigateTo({ path: '/login', query: { redirect: to.fullPath } })
    }

    // 已登录时访问登录页或注册页，跳转首页
    if (token && publicRoutes.includes(to.path)) {
      return navigateTo('/')
    }
  }
})
```

## 12. 核心业务代码阅读顺序

建议按以下顺序阅读源码：

### 12.1 基础认证流程

```text
client/pages/login.vue
  -> client/stores/auth.js
  -> client/composables/useApi.js
  -> AuthController.java
  -> AuthServiceImpl.java
  -> JwtUtil.java
  -> JwtAuthenticationFilter.java
  -> SecurityUtil.java
```

### 12.2 科目章节流程

```text
client/pages/subjects.vue
  -> SubjectController.java
  -> SubjectServiceImpl.java
  -> SubjectMapper.java
  -> subjects 表

client/pages/subject/[id].vue
  -> ChapterController.java
  -> ChapterServiceImpl.java
  -> ChapterMapper.java
  -> chapters 表
```

### 12.3 考试练习流程

```text
PracticeController.java
  -> AnswerChecker.java
  -> WrongBookServiceImpl.java
  -> AnswerRecordMapper.java
  -> WrongQuestionMapper.java

ExamController.java
  -> ExamServiceImpl.java
  -> ExamPaperMapper.java
  -> ExamRecordMapper.java
  -> AnswerRecordMapper.java
```

## 13. 代码注释总结

本项目代码整体采用前后端分离结构，后端核心是 Spring Boot 的三层架构，前端核心是 Nuxt 页面、Pinia 状态管理和统一 API 请求封装。

代码阅读时应重点关注：

- `SecurityConfig`：系统接口安全入口。
- `JwtAuthenticationFilter`：每个请求的登录态解析入口。
- `SecurityUtil`：用户数据隔离的基础。
- `AuthServiceImpl`：登录注册和 Token 生成逻辑。
- `SubjectServiceImpl`、`ChapterServiceImpl`：基础业务模块实现。
- `useApi.js`：前端请求统一封装。
- `auth.js`：前端登录态管理。
- `auth.global.js`：前端页面访问控制。

